apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stepik-teachers.fullname" . }}-test-connection"
  labels:
    {{- include "stepik-teachers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stepik-teachers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
