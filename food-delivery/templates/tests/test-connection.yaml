apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "food-delivery.fullname" . }}-test-connection"
  labels:
    {{- include "food-delivery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "food-delivery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
